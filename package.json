{
  "name": "hstore-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run \"--filter=@hstore/*\" build",
    "lint": "bun run \"--filter=@hstore/*\" lint",
    "test": "bun run \"--filter=@hstore/*\" test",
    "publish": "bun run scripts/publish/index.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "commander": "^14.0.2"
  }
}