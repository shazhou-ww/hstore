{
  "name": "@hstore/workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run \"--filter=@hstore/*\" build",
    "lint": "bun run \"--filter=@hstore/*\" lint",
    "test": "bun run \"--filter=@hstore/*\" test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.6.3"
  }
}